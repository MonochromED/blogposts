

<strong>Poster:</strong>
<%= @comment.poster %>
<br />
<div class="indexIdInfo">

    <strong>Comment ID:</strong>
    <%= @comment.id %>
    <br />


    <strong>Belongs to Review ID:</strong>
    <%= @comment.article_post_id %>

</div>

  <strong>Date:</strong>
  <%= @comment.date %>
  <br />

  <strong>Comment:</strong>
  <br />
  <%= @comment.comment %>
  <br />

<% comment_id = @comment.id %>
<% if controller.allow_access_if_owner_name_is_or_rank_at_least("#{@comment.poster}", 1 ) %>
  <%= link_to 'Edit', edit_comment_path(@comment), :commentid => "#{comment_id}"  %>|
  <%= link_to 'Delete', @comment, method: :delete, data: { confirm: 'Are you sure?'} %>
<% end %>

<br />
<%= link_to 'To Review Containing Comment', article_post_path(@comment.article_post_id) %>